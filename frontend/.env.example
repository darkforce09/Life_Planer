# Base URL of the Life Planner backend API (Expo public env var).
EXPO_PUBLIC_API_URL=http://localhost:3000

# Bearer token matching the backend's API_AUTH_TOKEN (leave blank if auth is disabled).
EXPO_PUBLIC_API_TOKEN=
